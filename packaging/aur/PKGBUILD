# Maintainer: ElevenLabs CLI Contributors
pkgname=elevenlabs-cli
pkgver=__VERSION__
pkgrel=0
pkgdesc="Unofficial CLI for ElevenLabs text-to-speech API"
arch=('x86_64' 'aarch64')
url="https://github.com/hongkongkiwi/elevenlabs-cli"
license=('MIT')
depends=('glibc')
makedepends=()
source_x86_64=("elevenlabs-cli-${pkgver}-x86_64.tar.gz::https://github.com/hongkongkiwi/elevenlabs-cli/releases/download/v${pkgver}/elevenlabs-cli-v${pkgver}-x86_64-unknown-linux-musl.tar.gz")
source_aarch64=("elevenlabs-cli-${pkgver}-aarch64.tar.gz::https://github.com/hongkongkiwi/elevenlabs-cli/releases/download/v${pkgver}/elevenlabs-cli-v${pkgver}-aarch64-unknown-linux-musl.tar.gz")
sha256sums_x86_64=('__SHA256_X86_64__')
sha256sums_aarch64=('__SHA256_AARCH64__')

package() {
  install -Dm755 "$srcdir/elevenlabs-cli" "$pkgdir/usr/bin/elevenlabs-cli"
}